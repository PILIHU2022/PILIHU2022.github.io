# 使用GitHub Actions自动部署Hugo博客
name: Hugo-Build
on:
  push:
    branches:
      - Hugo
jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2
      - name: Install Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-verson: latest
          extended: true
      - name: Hugo Build
        run: |
          git submodule update
          hugo
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< HEAD
          publish_dir: public
=======
          publish_dir: ./public
>>>>>>> 5bd1295 (Hugo:Changed files)
